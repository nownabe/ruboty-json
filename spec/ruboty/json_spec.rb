require 'spec_helper'

describe Ruboty::Json do
  it 'has a version number' do
    expect(Ruboty::Json::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
